<%= form_for(@sportgoods_category) do |f| %>
  <% if @sportgoods_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sportgoods_category.errors.count, "error") %> prohibited this sportgoods_category from being saved:</h2>

      <ul>
      <% @sportgoods_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit 'Сохранить', class: 'btn btn-primary' %>
  </div>
<% end %>
